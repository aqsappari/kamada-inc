<!DOCTYPE html>
<html>
  <head>
    <title>Basic EJS Example</title>
  </head>
  <body>
    <h1>Hello, <%= name %>!</h1>

    <h2>Items:</h2>
    <ul>
      <% items.forEach(item => { %>
      <li><%= item %></li>
      <% }); %>
    </ul>
  </body>
</html>
